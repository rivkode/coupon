# 로컬 개발 프로파일.
# 활성화: ./gradlew :server-b:bootRun --args='--spring.profiles.active=local'
spring:
  config:
    activate:
      on-profile: local
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.promotion: DEBUG
    org.springframework.web: DEBUG
