# 로컬 개발 프로파일.
# 활성화: ./gradlew :server-a:bootRun --args='--spring.profiles.active=local'
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://localhost:3306/server_a?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: promotion
    password: promotion

logging:
  level:
    com.promotion: DEBUG
    org.springframework.web: DEBUG
